!function(global,factory){ 
  if(typeof exports==="object" && typeof module!=="undefined") {
    module.exports = factory();
  } else if(typeof define==="function" && define.amd) {
    define("loveNotes",[],factory);
  } else {
    global.LoveNotes = factory();
  }
}(this, function(){ 
  "use strict";
  function initLoveNotes(){
    // Initialize gallery and scroll animations once DOM is ready.
    if(document.readyState === "loading"){
      document.addEventListener("DOMContentLoaded", function(){
        initGallery();
        initScrollingAnimation();
      });
    } else {
      initGallery();
      initScrollingAnimation();
    }
  }
  function initGallery(){
    console.log("Initializing LoveNotes Gallery");
    // ...existing gallery initialization code (e.g., from desktop-gallery.js/mobile-gallery.js)...
  }
  function initScrollingAnimation(){
    console.log("Initializing scrolling animations");
    // ...existing scrolling animation code (e.g., from scripts.js)...
  }
  return {
    init: initLoveNotes
  };
});
